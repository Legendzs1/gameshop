extends layout

block content
  div(class="home-content .bg-dark jumbotron jumbotron-fluid " style="background-color:#343a40")
    div(class="container ")
      h1(class="display-4 text-white") #{title}
      p(class='text-white') Welcome to #[em Game Store], a very basic Express website developed as a project based off The Odin Project.

      h1(class='text-white') Dynamic content

      if error
        p(class='text-white') Error getting dynamic content.
      else
        p(class='text-white') The game store has the following record counts:

        ul
          li(class='text-white') #[strong Games:] !{data.game_count}
          li(class='text-white') #[strong Genres:] !{data.genre_count}
