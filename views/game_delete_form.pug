extends admin_layout

block content
  h1 #{title}

  form(method='POST' action='')
    div.form-group
      label(for='name') Search for Game to delete:
      input#name.form-control(type='text', placeholder='League of Legends, PUBG etc.' name='name')
    button.btn.btn-primary(type='submit') Submit

  if errors 
    p= errors

